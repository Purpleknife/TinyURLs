import React from 'react';

interface LoginProps {
  handleClose: () => void;
  show: boolean;
}

const Login = (props: LoginProps) => {
  return (
    <div>
      Login
    </div>
  );
}
 
export default Login;